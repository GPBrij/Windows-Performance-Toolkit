# Usage

Run diagnostic scripts individually from `src`. Store generated reports outside the repository.

```powershell
Set-Location .\src
.\SystemUpgradeAudit.ps1
```

Review every recommendation against the device role, workload, and change-control requirements.
